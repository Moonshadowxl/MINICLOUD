{
  "name": "@minicloud/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MiniCloud desktop sync agent — keeps local folders synced to your MiniCloud on a schedule, survives the server being down.",
  "bin": { "minicloud-agent": "dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/index.ts",
    "agent": "tsx src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
